<%- include("partials/header"); -%>

<h1>Home</h1>

<p><%= homeStartingContent %></p>

<!-- <% for(var i=0; i<posts.length; i++){ %>
    <h4><%= posts[i].title %></h4>
    <p><%= posts[i].content %></p>
<% } %> -->


<% posts.forEach(function(element){ %>
    <h1><%= element.title %></h1>
    <p><%= _.truncate(element.content, {length:100}) %>
    <% const temp = "/posts/:" + element.title; %>
    <!-- <% console.log(temp); %> -->
    <a href="<%= temp %>">Read More...</a>
    </p>
<% }) %>

<%- include("partials/footer"); -%>